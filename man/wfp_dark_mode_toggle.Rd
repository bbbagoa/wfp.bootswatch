% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/wfp_theme.R
\name{wfp_dark_mode_toggle}
\alias{wfp_dark_mode_toggle}
\title{Dark Mode Toggle for WFP Theme}
\usage{
wfp_dark_mode_toggle(
  id = "wfp_dark_mode",
  label = NULL,
  icon = NULL,
  class = "btn-sm"
)
}
\arguments{
\item{id}{The input ID for the toggle. Default: \code{"wfp_dark_mode"}.}

\item{label}{Label text. Default: \code{NULL} (icon only).}

\item{icon}{Icon to display. Default: a moon/sun icon.}

\item{class}{Additional CSS classes. Default: \code{"btn-sm"}.}
}
\value{
A Shiny UI element (button) that toggles dark mode.
}
\description{
Creates a toggle button or switch that allows users to switch between
light and dark modes at runtime. This function generates the UI for
a dark mode control.
}
\details{
The toggle button works by setting the \code{data-bs-theme} attribute on
the \code{<html>} element between \code{"light"} and \code{"dark"}. This
triggers the dark mode CSS rules that are included when
\code{wfp_theme(dark_mode = TRUE)} or \code{wfp_theme(dark_mode = "auto")}
is used.

The toggle state is persisted in the browser's \code{localStorage} so that
the user's preference is remembered across sessions.

When Shiny is available, the toggle sends its state to the server as
a logical input value.
}
\examples{
\dontrun{
library(shiny)
library(wfp.bootswatch)

ui <- fluidPage(
  theme = wfp_theme(dark_mode = "auto"),
  wfp_dark_mode_toggle(),
  h1("My App")
)

server <- function(input, output, session) {
  observeEvent(input$wfp_dark_mode, {
    # The toggle is handled automatically by JavaScript
    # But you can react to the change here if needed
    message("Dark mode: ", input$wfp_dark_mode)
  })
}
}
}
\seealso{\code{\link{wfp_theme}}, \code{\link{wfp_toggle_dark_mode}}}
